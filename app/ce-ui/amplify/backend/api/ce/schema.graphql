type CEPlayer @model @key(fields: ["name"]) {
  name: String!
  class: String
  rank: String
}

type CEStandings @model @key(fields: ["player_name","recorded"]) {
  player_name: String!
  recorded: Float!
  ep: Float
  gp: Float
  priority: Float
}
